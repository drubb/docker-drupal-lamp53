If you want to connect to external servers from inside your container,
e.g. git repos, place your ssh keys inside this folder.

At the moment, they have to be named "id_rsa" and "id_rsa.pub",
or you'll have to modify the Dockerfile otherwise.
